# 审稿意见修改 — 可贴回 docx 的替换文字与说明

> 配套：`审稿修改_追踪表.md`。本文件给出 **16 条逐条替换文字**，可直接复制回 `流式架构的级联式语音对话系统延迟优化.docx`。
> 决议推断：**小修**；**16 条全部无需补做实验**（C10/C15 用工程依据与已有结果复核替代可选实验）。
>
> **使用方法**
> 1. 「原文」块 = 用来在 Word 中定位的现有文字（可用 Ctrl+F 搜索）。
> 2. 「改为／新增」块 = 复制这段贴回去。
> 3. 数学符号（如 T_total、O(L²)）请在 Word 中按正式上下标格式排版；式(1)符号请与你 MathType 公式里的实际下标核对。
> 4. **🟡 占位符** 标记处（C13、C15）必须填你的真实实验值，切勿照抄占位文字。
> 5. 建议在 Word「修订模式」下修改或用黄色高亮，便于审稿人复核。

---

## 修改总览

| # | 位置 | 操作 | 一句话 |
|---|---|---|---|
| C4 | 引言·式(1)后 | 新增 | 符号定义 + TTFT 口径 |
| C12 | 3.4.3 | 替换 | 多轮语料·单次输入时长有限 |
| C15 | 4.4.2 | 替换 | WER=0 来源与对齐说明 |
| C13 | 4.2.1 | 新增 | 推理配置段（填实测值🟡） |
| C1 | 题名/摘要/页眉 | 全局替换 | 统一"流水线并行流式" |
| C9 | 3.1.1 | 替换 | 图3-1 → 图3（全文图号统一） |
| C2 | 中文摘要 | 替换 | 补 1132 条·≥15s 分组口径 |
| C7 | 2.1.1 | 替换 | Mel 维度定为 128（对齐 Turbo） |
| C8 | 2.2.2 | 新增/替换 | 两层机制显式区分 |
| C10 | 3.2.2 | 替换 | VAD 三阈值工程依据 |
| C14 | 4.2.2 | 新增/替换 | 改进%定义 + "负向优化"改称 |
| C6 | 1.4 | 替换 | 指代不清改写 + 引 3.4 |
| C3 | 引言 | 替换(酌情) | 压缩前沿背景、前置问题 |
| C5 | 1.1 | 替换 | "没法并行化"改写 |
| C11 | 3.3.1 | 替换 | "边听边算"改学术表达 |
| C16 | 5.1 | 替换+加引用 | 200ms 补来源[22] |

---

# P1 — 必改

## C4　公式(1)符号定义 + TTFT 统计口径【唯一强制项】

> **批注**：公式(1)后必须补充各延迟项的符号定义，必须确保公式中的所有变量均有说明！并说明网络延迟、TTS延迟是否纳入本文TTFT统计口径。

**位置**：引言，公式(1)正下方（"在大多数常见的传统级联式实现中…"一段之前）
**操作**：**新增**一段（紧接式(1)）。

**新增（复制贴入）：**
```
式(1)中，T_total 为端到端总延迟，T_VAD、T_ASR、T_LLM、T_TTS 分别表示语音端点检测（VAD）、自动语音识别（ASR）、大语言模型推理（含预填充 prefill 与首 token 解码）以及语音合成（TTS）各阶段的处理延迟。需要说明的是，本文研究目标为端点后等待，故将首字延迟（TTFT，定义见 2.3.1 节）严格界定为 End-of-Speech 至 LLM 输出首个响应 token 的时间差，仅涵盖 ASR 尾部处理、LLM 增量预填充与首 token 解码三部分；TTS 合成与音频播放不计入 TTFT 统计口径。此外，本文原型为单机本地部署，模块间经进程内线程安全队列通信，网络传输延迟可忽略，故亦未计入。
```
**说明**：① 一次性满足"所有变量均有说明"；② 明确回答审稿人"网络/TTS 是否计入"＝**不计入**，并给出理由；③ 纯文字，无需实验。
**⚠️ 核对**：T_VAD/T_ASR/T_LLM/T_TTS 的下标须与你 MathType 式(1)里实际使用的符号一致；若式中还含网络项（如 T_net），请一并加入定义。

---

## C12　数据集描述事实更正

> **批注**：MultiWOZ/CrossWOZ并非普通单轮语料，建议改为"单次用户输入时长有限"等更准确表述。

**位置**：3.4.3 累积对话生成逻辑
**操作**：**替换**整句。

**原文：**
> 由于原始数据集中均为单轮对话，一次输入的时长有限。为了模拟用户进行长语音输入的真实场景(例如用户一口气陈述复杂的旅行需求，或在对话中不断补充信息)，本实验采用了"累积对话"策略。

**改为（复制贴入）：**
```
MultiWOZ 与 CrossWOZ 虽为多轮任务型对话语料，但其单条用户语句时长有限，难以直接构造长语音输入。为模拟用户进行长语音输入的真实场景（例如用户一口气陈述复杂的旅行需求，或在对话中不断补充信息），本实验采用"累积对话"策略，将多轮对话历史逐轮拼接为长度递增的输入序列。
```
**说明**：审稿人指出属实——两者均为**多轮**任务型对话数据集；且你的累积策略恰恰利用其多轮结构，更正后逻辑更顺。纯文字。

---

## C15　System A 的 WER/CER=0 说明

> **批注**：System A WER/CER为0可能引发疑问，建议说明参考文本来源、对齐方法以及是否经过人工抽查。

**位置**：4.4.2 精度-延迟分析（首段）
**操作**：**替换**相关句。

**原文：**
> 由于评测音频由文本经 TTS 合成生成，参考文本可视为该批样本的近似真值；同时，System A在该合成集合上的 WER/CER为0，作为上界对照。本文主要关注不同上下文配置下的相对差异。

**改为（复制贴入）：**
```
本实验的参考文本直接取自 MultiWOZ/CrossWOZ 的原始转写文本，即用于 TTS 合成的输入文本，可视为该批合成语音的真值（Ground Truth）。WER/CER 采用标准 Levenshtein 编辑距离对齐计算，计算前对文本统一进行大小写、标点与空白归一化。System A 为非流式全量识别：对完整音频一次性离线解码，不存在流式切片的边界截断，因而在与参考文本同源的干净合成语音上可近乎逐字复原输入文本，其 WER/CER 近似为 0，作为质量上界对照。需说明的是，该上界源于"合成语音—参考文本同源"，真实录音场景下的绝对误差会高于此值；故本文主要关注不同上下文配置之间的相对差异。
```
**说明**：把"为何为 0"讲透（同源 + 全量离线解码），并补充对齐方法。**无需重测**。
**🟡 二选一**：
- 若你当时**做过**人工抽查：在"作为质量上界对照"后加一句"并随机抽查 N 条转录人工确认无系统性错误"（N 填实际条数）。
- 若**未抽查**：建议把正文与表5上界由"为 0"改为"近似为 0"，或报告实测极小值（这是核对已有转录输出，非新实验）。

---

## C13　补充推理关键配置

> **批注**：建议补充推理精度、batch大小、最大输出token、是否启用FlashAttention或vLLM等关键配置。

**位置**：4.2.1 实验设置与环境构建，公平性控制段末（"…而非模型权重或硬件条件。"之后）
**操作**：**新增**一段。

**新增（复制贴入，🟡 处填实测值）：**
```
在推理配置方面，对比组与实验组采用完全一致的设置以保证可比性：模型推理精度为 🟡【BF16 / FP16，请按实际填写】，在线推理 batch size 固定为 1（单请求实时处理），最大生成长度为 🟡【max_new_tokens = N，请填写】；注意力实现为 🟡【PyTorch SDPA / eager / FlashAttention-2，请按实际填写】。系统基于 HuggingFace Transformers 原生推理接口（use_cache / past_key_values）实现 KV Cache 复用，未引入 vLLM、TGI 等独立推理服务框架，以确保 System A 与 System B 在相同底层实现下仅因流式调度与增量更新方式而产生差异。
```
**说明**：只是把你**已经用过**的配置写出来，提升可复现性，无需新实验。
**🟡 必填**：三处占位符按你的真实运行参数填写；若实际**用了** vLLM/FlashAttention，请相应改写最后一句（不要照抄"未引入"）。

---

## C1　术语统一："流水线并行流式"

> **批注**：题名与摘要、正文中的"流水线并行"表述需保持一致；建议核定是否将"并行流式"统一为"流水线并行流式"，并同步检查页眉题名信息。

**操作**：**全局统一**。英文题名已为 *Pipeline-Parallel Streaming Architecture*，故中文统一为"**流水线并行流式架构**"。

**① 题名**
**原文：** `并行流式架构的级联式语音对话系统延迟优化`
**改为：**
```
流水线并行流式架构的级联式语音对话系统延迟优化
```

**② 摘要首句**（统一术语）
**原文：** `…提出流水线并行的流式优化架构，使语音识别、语言模型推理可重叠执行。`
**改为：**
```
…提出流水线并行流式架构，使语音识别、语言模型推理可重叠执行。
```

**③ 页眉**：将页眉中的题名/简称同步改为与新题名一致（审稿人特别点名）。
**④ 核对**：关键词"流水线并行"、英文标题、各处正文表述保持统一；文件名可酌情改为含"流水线并行流式"。
**说明**：纯文字一致性，无需实验。

---

## C9　图号统一：图3-1 → 图3

> **批注**：正文编号"图3-1"与图题"图3"不一致，需统一全文图号和交叉引用。

**位置**：3.1.1 目标与逻辑拓扑
**操作**：**替换** + 全文核对。

**原文：**
> 系统的逻辑拓扑如图 3-1 所示。

**改为（复制贴入）：**
```
系统的逻辑拓扑如图 3 所示。
```
**说明**：全文采用扁平编号（图1–图6、表1–表5）。请用 Ctrl+F 搜索"图"逐一核对所有"如图X所示"与图题、表题编号一致，无"X-Y"式残留。纯文字。

---

# P2 — 应改

## C2　摘要补统计基准

> **批注**：摘要中的降幅和最长分组减少值比较关键…建议补充测试集规模或分组口径。

**位置**：中文摘要
**操作**：**替换**该句（英文摘要可同步补一句，见说明）。

**原文：**
> 长语音数据集实验表明，长语音分组的平均首token输出时间稳定在约1.1秒，相比非流式基线降低34.6%–83.9%，最长分组平均减少5.67秒；转录错误率保持在可接受范围。

**改为（复制贴入）：**
```
在由 MultiWOZ（英文）与 CrossWOZ（中文）合成的 1132 条递增长度语音测试集上（按音频时长分组，15 s 以上为长语音区间），长语音分组的平均首 token 输出时间稳定在约 1.1 秒，相比非流式基线降低 34.6%–83.9%，最长分组平均减少 5.67 秒；转录错误率保持在可接受范围。
```
**说明**：样本规模（1132 条，见 4.2.1）与分组口径（按时长、≥15s，见表2/表3）均为已有数据。**英文摘要**可同步补 "on 1,132 progressively-lengthening utterances synthesized from MultiWOZ and CrossWOZ"。无需实验。

---

## C7　Whisper 版本与 Mel 维度

> **批注**：建议注明所用Whisper版本对应的Mel维度；若实验实际使用Whisper-Turbo，应与后文模型配置保持一致。

**位置**：2.1.1 Whisper 模型架构与离线特性
**操作**：**替换**该句。

**原文：**
> 如图1所示，Whisper的输入端将16 kHz音频转换为80/128维Log-Mel频谱特征，经卷积前端下采样后送入Transformer编码器；

**改为（复制贴入）：**
```
如图1所示，Whisper 的输入端将 16 kHz 音频转换为 128 维 Log-Mel 频谱特征（与本文实验所用 Whisper-Turbo 一致；早期 Whisper 模型采用 80 维），经卷积前端下采样后送入 Transformer 编码器；
```
**说明**：4.2.1 使用 Whisper-Turbo（large-v3-turbo，128 维 Mel），此处与之对齐，消除"80/128"歧义。无需实验。

---

## C8　区分"解码 KV 复用"与"增量 prefill 前移"

> **批注**：建议进一步区分"解码阶段KV Cache复用"和"增量prefill前移计算"两层机制，避免读者误解为复杂度阶数降低。

**位置**：2.2.2 段（"本文提出的增量预填充策略并不改变复杂度阶数…"句之前插入）
**操作**：**新增**一句承接（并可略改原句开头避免重复）。

**在原文该句前插入（复制贴入）：**
```
此处需区分两个层面的机制：其一为解码阶段的 KV Cache 复用，它将自回归生成中每一步的计算由与序列长度二次相关降为线性相关，属现代推理引擎的通用优化；其二为本文在预填充阶段引入的增量计算，它并不降低注意力计算的复杂度阶数——若将增量片段累积到最终长度 L，其累计开销仍与一次性 prefill 同阶（约 O(L²)）——而是利用 ASR 文本逐步到达的时间特性，将 prefill 计算在时间上前移并与用户发声过程重叠。
```
**说明**：你正文已写"并不改变复杂度阶数"，此处把两层机制点明，杜绝"降阶"误读。可顺势将紧随的"本文提出的增量预填充策略并不改变复杂度阶数，而是…"精简为"具体而言，该策略…"以免重复。纯文字。

---

## C10　VAD 三阈值的选择依据

> **批注**：建议说明500 ms块长、2 s最小语音、300 ms静音阈值的选择依据，必要时增加参数敏感性说明。

**位置**：3.2.2 动态 VAD 分段策略（末句）
**操作**：**替换**该句。

**原文：**
> 本文原型系统的实验配置为音频块长度500 ms、最小语音长度2s、最小静音时长300 ms，以在实时性与稳定性之间取得折中。

**改为（复制贴入）：**
```
本文原型系统的实验配置为音频块长度 500 ms、最小语音长度 2 s、最小静音时长 300 ms。其取值依据如下工程考量：500 ms 的音频块在 VAD 检测的响应粒度与调用开销之间取得平衡；2 s 的最小语音长度用于抑制过短切片造成的频繁触发与声学上下文不足；300 ms 的静音阈值与自然对话中常见的话轮停顿时长相当，可在尽早闭合语音段与避免误切之间折中。上述取值与 Silero VAD 的常用经验配置一致；更系统的参数敏感性分析留待后续工作。
```
**说明**：审稿人"必要时"增加敏感性分析——本文用**工程依据**回应、并将敏感性列为后续工作即可，**无需补实验**。

---

## C14　"改进%"定义 + "负向优化"改称

> **批注**：建议明确负向优化的计算公式和单位，或改写为"延迟增加"，避免读者误解为系统错误。

**位置**：4.2.2 延迟趋势分析
**操作**：**新增**定义句 + **替换**"负向优化"措辞。

**① 在表3首次出现"改进(%)"处补定义（复制贴入，可作表3脚注或正文）：**
```
表中"改进"定义为相对延迟降幅：改进(%) =（非流式 TTFT − 流式 TTFT）/ 非流式 TTFT × 100%；正值表示 System B 相对基线缩短延迟，负值表示 System B 延迟增加。
```

**② 替换"负向优化"表述**
**原文：**
> 值得一提的是short与medium组在System B下分别录得114.75ms与36.11ms的负向优化结果，这表明在短语音场景下…

**改为（复制贴入）：**
```
值得一提的是，short 与 medium 组在 System B 下分别录得 114.75 ms 与 36.11 ms 的延迟增加（即改进百分比为负），这表明在短语音场景下…
```
**说明**：定义来自现有数据反推（已核对：Short (533.42−648.17)/533.42 = −21.5% 与表3一致）。全文凡"负向优化"统一改为"延迟增加"。无需实验。

---

## C6　测试基准指代不清

> **批注**：此句指代不清，建议明确测试基准的构建方式，如TTS合成、累积对话长度划分和样本规模。

**位置**：1.4 本文主要研究内容（第三点）
**操作**：**替换**该句。

**原文：**
> 本文在MultiWOZ（英文）和CrossWOZ（中文）数据集上构建了长语音测试基准，这是对该内容进行全链路延迟评估与验证的工作。

**改为（复制贴入）：**
```
本文基于 MultiWOZ（英文）与 CrossWOZ（中文）构建了长语音测试基准，并据此开展全链路端到端延迟评估（测试集的 TTS 合成、累积对话长度划分与样本规模等构建细节见 3.4 节）。
```
**说明**：消除"这是…的工作"的悬空指代，并把审稿人点名的三项细节交叉引用到 3.4（那里已有）。纯文字。

---

# P3 — 酌改（措辞/结构）

## C3　压缩前沿背景、前置端点后等待问题（酌情）

> **批注**：该处前沿模型背景建议略微压缩，建议更早引出级联式系统在长语音场景中的端点后等待问题。

**位置**：引言第 2 段
**操作**：**精简**GPT-4o/Gemini 细节 + 段末**加一句**问题前置。

**原文（第2段）：**
> …公开的系统报告指出，GPT - 4o端到端语音响应延迟可低至232毫秒，平均320毫秒[1]，这已经接近人类自然对话反应速度。这意味着，"低延迟"与"准确率"并列，是下一代语音交互体验的关键竞争指标。

**改为（复制贴入）：**
```
…公开报告显示，GPT-4o 的端到端语音响应延迟平均约 320 ms[1]，已接近人类自然对话的反应速度，"低延迟"与"准确率"由此并列为下一代语音交互的关键指标。然而对广泛采用的级联式系统而言，长语音场景下"端点后等待时间随输入时长增长"的问题尤为突出，正是本文的优化对象。
```
**说明**：删去 232ms/平均320ms 的并列冗述，提前点出本文要解决的"端点后等待"问题。此为编辑性优化，可酌情采纳。纯文字。

---

## C5　"没法并行化"改写

> **批注**："没法并行化"表述过于绝对，建议改为"并行训练效率受限"或补充具体限制条件。

**位置**：1.1 自动语音识别的流式化进展
**操作**：**替换**该句。

**原文：**
> RNN-T凭借其天然的循环神经网络的特性，非常适合处理流式音频，但其没法并行化，因此训练效率低，长程语义捕捉困难。

**改为（复制贴入）：**
```
RNN-T 凭借循环神经网络的天然时序特性，非常适合处理流式音频，但其逐时刻的循环依赖使训练时难以在时间维度上充分并行，并行训练效率受限，且长程语义捕捉较为困难。
```
**说明**：把绝对化的"没法并行化"改为有条件的"训练时难以在时间维度上并行/效率受限"。纯文字。

---

## C11　"边听边算"改学术表达

> **批注**：该句口语化较明显，建议改为学术表达，并将"边听边算"表述为"音频输入期间的增量计算"。

**位置**：3.3.1 策略说明
**操作**：**替换**该句。

**原文：**
> 要是把这种范式直接放到"边听边算"的增量输入场景里，就会白白浪费End-of-Speech之前的宝贵时间。

**改为（复制贴入）：**
```
若将该范式直接套用于音频输入期间的增量计算场景，则会闲置 End-of-Speech 之前本可用于计算的时间窗口，造成资源浪费。
```
**说明**：去口语化，按审稿人指定用语替换。纯文字。

---

## C16　"约200ms对话间隙"补来源

> **批注**：建议为"约200ms对话间隙"补充来源，或改为更审慎的经验性表述。

**位置**：5.1 全文总结
**操作**：**替换**该句（加引用）+ 参考文献追加 [22]。

**原文：**
> 虽然在long及以上分组的TTFT仍然有约为1.1s，与人类对话中约200ms的对话间隙仍有一定差距，

**改为（复制贴入，推荐）：**
```
虽然在 long 及以上分组的 TTFT 仍约为 1.1 s，与人类自然对话中约 200 ms 的话轮转换间隙[22]仍有一定差距，
```
**参考文献追加（复制贴入，作为 [22]）：**
```
[22] STIVERS T, ENFIELD N J, BROWN P, et al. Universality and cultural variation in turn-taking in conversation[J]. Proceedings of the National Academy of Sciences, 2009, 106(26): 10587-10592.
```
**说明**：~200ms 话轮间隙的经典来源为 Stivers et al. (2009, PNAS)。该文献编号在结论处引用，置于文末 [22] 不影响前序编号。
**🟡 核对**：请确认正文与参考文献表编号衔接（现有为 [1]–[21]）。若不想动参考文献，可改为审慎表述："与人类自然对话中数百毫秒量级的话轮间隙仍有一定差距"（去掉[22]）。

---

# 回复信（Response to Reviewers）— 逐条草稿

> 复制后将 **[填写位置]** 换成 docx 中的页/段；🟡 处按实际填写。语气：先致谢 → 已采纳 → 具体改动 → 位置。

```
尊敬的编辑与审稿专家：

衷心感谢您对本文《流水线并行流式架构的级联式语音对话系统延迟优化》的细致审阅。
您提出的 16 条意见显著提升了本文的严谨性、一致性与可读性。我们已逐条修改，
修订处在稿件中以修订模式/黄色高亮标出。以下为逐条回复（先列审稿意见，再述处理）。

— 一、强制性意见 —

【意见4】公式(1)后须补各延迟项符号定义，并说明网络/TTS 延迟是否计入 TTFT。
  回复：已采纳。已在式(1)后新增符号定义（T_total/T_VAD/T_ASR/T_LLM/T_TTS），
        并明确 TTFT 仅含 ASR 尾部处理、LLM 增量预填充与首 token 解码，
        不含 TTS 与音频播放；单机本地部署网络延迟可忽略，未计入。位置：[填写位置]

— 二、一致性类 —

【意见1】题名/摘要/正文"流水线并行"表述统一，并核查页眉。
  回复：已将中文统一为"流水线并行流式架构"，并同步题名、页眉、摘要、关键词与
        英文标题。位置：题名/页眉/[填写位置]
【意见9】"图3-1"与"图3"不一致。
  回复：已统一全文图号（图1–图6）与交叉引用。位置：[填写位置]
【意见7】注明 Whisper 版本对应 Mel 维度，并与 Whisper-Turbo 一致。
  回复：已明确为 128 维 Log-Mel，并与 4.2.1 模型配置统一。位置：[填写位置]
【意见8】区分"解码 KV Cache 复用"与"增量 prefill 前移"。
  回复：已新增两层机制的显式区分，强调本文策略不改变复杂度阶数、仅前移计算。
        位置：[填写位置]
【意见14】明确"负向优化"的计算公式与单位。
  回复：已给出改进百分比定义（(非流式−流式)/非流式×100%），并将"负向优化"
        统一改为"延迟增加(ms)"。位置：[填写位置]

— 三、完整性/可信度类 —

【意见2】摘要补测试集规模/分组口径。
  回复：已在摘要补充"1132 条递增长度语音测试集、按时长分组、≥15s 为长语音区间"。
        位置：摘要
【意见13】补推理精度/batch/最大 token/是否启用 FlashAttention 或 vLLM。
  回复：已补充推理配置：精度 🟡__、batch=1、max_new_tokens=🟡__、注意力实现 🟡__，
        基于 HuggingFace 原生 use_cache/past_key_values，未使用 vLLM/TGI。位置：[填写位置]
【意见15】System A 的 WER/CER=0 说明。
  回复：已说明参考文本来源（TTS 输入文本即真值）、Levenshtein 对齐与归一化方法，
        并解释 System A 全量离线解码在同源合成语音上近乎逐字复原故近似为 0；
        🟡【已/将随机抽查 N 条人工确认】。位置：[填写位置]
【意见6】测试基准构建方式指代不清。
  回复：已改写该句并交叉引用 3.4 节（TTS 合成、累积对话划分、样本规模）。位置：1.4 节
【意见12】MultiWOZ/CrossWOZ 非单轮语料。
  回复：已更正为"多轮任务型语料、单次用户语句时长有限"，并据此说明累积对话策略。
        位置：3.4.3
【意见10】500ms/2s/300ms 阈值依据。
  回复：已补充三项阈值的工程选择依据；更系统的参数敏感性分析作为后续工作说明。
        位置：3.2.2

— 四、表达/结构类 —

【意见3】压缩前沿模型背景、更早引出端点后等待问题。
  回复：已精简 GPT-4o/Gemini 延迟数据的铺陈，并在引言提前点出级联系统长语音
        端点后等待问题。位置：引言
【意见5】"没法并行化"过于绝对。
  回复：已改为"循环依赖使训练时难以在时间维度上并行、并行训练效率受限"。位置：1.1 节
【意见11】"边听边算"口语化。
  回复：已改为"音频输入期间的增量计算"。位置：3.3.1 节
【意见16】"约200ms对话间隙"补来源。
  回复：已补引用 Stivers 等(2009, PNAS)作为[22]【或改为"数百毫秒量级"审慎表述】。
        位置：5.1 节、参考文献

我们相信上述修改已充分回应各条意见。如需进一步调整，我们乐于配合。

作者敬上
2026-__-__
```

---

## 修改后自查（提交前）

- [ ] C4 符号下标与 MathType 式(1)逐一核对一致
- [ ] C13 三处 🟡 已填实测值；若用了 vLLM/FlashAttention，已改写末句
- [ ] C15 "为0/近似为0"已定调；如声明抽查，N 已填
- [ ] C1 术语统一覆盖题名/页眉/摘要/关键词/正文/英文标题
- [ ] C9 全文无"图X-Y"残留，图1–图6 与表1–表5 交叉引用一致
- [ ] C16 参考文献 [22] 已加且编号衔接无误（或已改审慎表述、不加引用）
- [ ] 回复信 16 条与稿件改动、位置一一对应
- [ ] 改动均在"仅回应审稿意见"范围内，未引入未经请求的新内容/新章节
