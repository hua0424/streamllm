# 对 review-20260818-r4.md 的整改回复

**回复日期**：2026-08-18
**整改内容**：R4-P1-1 / R4-P1-2 / R4-P2-1 全部处理
**可复现测试命令**：

```bash
uv run python -m experiments.scripts.test_revision_regressions
# 10/10 passed（新增 B5 纯函数四用例、B6 生产路径三场景）
uv run python -m py_compile experiments/scripts/*.py src/asr/*.py
uv run python -m experiments.scripts.make_exp2_clean_source \
  --expected-clean-count 498 --expected-exclusion-count 7
```

---

## R4-P1-1：生产 endpoint 字段未自动回归 —— 已补（双层覆盖）

采纳评审建议的纯函数方案，并加做一层生产路径 stub 测试：

**第 1 层（B5，纯函数）**：`run_exp_latency.py` 新增
`classify_endpoint_times(speech_end_time, final_speech_segment_commit_time,
final_is_final_segment_enqueue_time, existing_error)`，`_run_streaming_test` 的
`asr_no_speech` 判定改走该函数（生产代码与测试同源）。四用例：正常语音（两 final 时间非零、
无错误）、flush-only（均非零）、全静音（`asr_no_speech`、detection_wait=None）、
顺序非法（`endpoint_timing_invalid`）。

**第 2 层（B6，生产路径 stub）**：fake ASR/LLM（仅满足调用协议，无模型依赖）驱动
**完整 `run_single_sample` 路径**——真实线程闭包、真实 StreamAudioSegmenter、真实
`--append-silence-ms` 拼接、真实 `classify_endpoint_times` 接线，输入为仓库内 fixture。
三场景断言并打印原始字段：

```text
[B6-1] speech_end=…78.279 speech_commit=…78.406 final_enqueue=…80.396
       detection_wait=0.127s enqueue_wait=2.117s   （正常语音+2s 尾静音）
[B6-2] speech_commit=…85.294 final_enqueue=…85.294（flush-only，两者同为 flush 时刻）
[B6-3] speech_commit=0 final_enqueue=…91.105 error=asr_no_speech（全静音）
```

不变量断言：三字段非零（除无语音场景的 speech_commit）、
`final_speech <= final_is_final`、`final_speech > speech_end`（VAD 闭段不得早于真实语音结束）、
`0 <= detection_wait < 2.0`、错误标记正确。

**过程中该测试抓住了一个真实测试设置缺陷**（佐证其有效性）：最初版本绕过
`run_single_sample` 手工预拼静音，`speech_end_time` 被记到流末尾，出现
`detection_wait=-2.007s` 的负值——改为走完整生产 append 路径后修复。此类接线下错误正是
B6 要防的风险类别。

## R4-P1-2：tiny E2E 证据不可复现 —— 已补（采纳方案 A）

- `experiments/results/revision/regression/endpoint_smoke.json`：最小可审计 artifact，
  含 sample_id、config、三个时间字段原始值、audio_end_time、
  endpoint_detection_wait（0.215s）、final_enqueue_wait（2.083s）、ttft、error、转写摘要；
- `endpoint_smoke.runinfo.md`：完整命令行、时间戳、"一次性冒烟非自动回归"的定位说明，
  以及指向 B6（可复现的自动回归等价物）的引用；
- 同时明确证据分层：**仓库级自动回归 = B6（fake 模型，可复现）；tiny E2E = 一次性人工
  冒烟 artifact（本文件）**。上轮 response-r3 中"真机 e2e 已验证"的表述按本轮口径理解为
  一次性验证，自动回归覆盖由 B6 承担。

## R4-P2-1：B2 未断言 flush-only 性质 —— 已补

B2 增加 `assert mids == []`（fixture 的 flush-only 性质显式化；该性质在 fixture 选取时
已按 16-bit 量化 round-trip 验证稳定）。若未来 Silero 版本漂移导致 fixture 产生中途闭段，
该断言会显式失败，届时按评审建议改名 "flush residual speech" 并重定义验收条件。

## 回归汇总

- `test_revision_regressions.py`：**10/10 PASS**（A1-A3 / B1-B6 / C1）；
- `py_compile experiments/scripts/*.py src/asr/*.py` 通过；
- 归档审计 498/7 通过；
- 新增/修改文件：`run_exp_latency.py`（classify_endpoint_times 抽取与接入）、
  `test_revision_regressions.py`（B5/B6/B2 增强）、
  `experiments/results/revision/regression/endpoint_smoke.json` + `.runinfo.md`。

E5 前置条件（R4-P1-1/P1-2）已满足，E3/E5 正式 GPU 实验均可启动。
