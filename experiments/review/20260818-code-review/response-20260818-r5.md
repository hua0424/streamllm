# 对 review-20260818-r5.md 的回复

**回复日期**：2026-08-18
**结论**：第五轮审查通过，全部事项关闭，无新增整改项。

## 一、关于 §三"测试与剩余风险说明"

四条边界说明全部认可，处理如下：

1. **B6 fake ASR/LLM 的验证边界**——认可。B6 定位即"线程/计时 bookkeeping/错误分类的接线回归"，真实模型质量与调度由 E3/E5 运行结果与 §5.2 QA 线把关。该边界已写入 response-r4 与本轮 r5 文档，无需再改。
2. **tiny E2E artifact 不作为论文数字**——认可。endpoint_smoke.runinfo.md 中已明确其为一次性冒烟；论文延迟数字只来自 GPU 机锁定配置的正式运行。
3. **回归套件的 Silero VAD 缓存依赖**——已处理：handoff"新鲜环境注意"新增第 3 条，说明 `test_revision_regressions.py`（预期 10/10）与 silero 预缓存共用同一 torch hub 缓存/网络要求，并建议正式实验前先跑一遍。
4. **正式 E5 结果的四项检查**——已处理：handoff §5.2 QA 验收表新增"E5 端点结果"行，含 (a) `audio_end_time − speech_end_time ≈ 2s`、(b) endpoint 统计排除 `asr_no_speech`、(c) 逐样本时间排序离线可核验、(d) 异常登记 `REVISION_CHANGELOG.md`。

## 二、改动清单

- `experiments/GPU_EXPERIMENT_HANDOFF.md`：上述两处补充（纯文档，无代码变更）；
- 本回复文档。

## 三、后续

E3/E5 正式 GPU 实验代码审查阻塞已全部解除。GPU 机按 handoff 执行（建议顺序：先跑
`test_revision_regressions.py` 确认 10/10，再开始 E 系列），正式结果按 §5.2 QA 线自查并
登记 changelog。
