# 对 review-20260818.md 的整改回复

**回复日期**：2026-08-18
**整改 commit**：`60683b9`（含此前 DEV 系列 commit `36e63ba`、`1f2d6c0`、`32c1a68`、`d05ae55`）
**总体结论**：9 项意见中 **8 项全部接受，1 项部分接受（P0-3，含技术澄清），0 项拒绝**。全部 P0 项已通过本机小模型冒烟回归，单元/负例测试齐全。评审状态表（原文 §7）已逐项更新。

---

## 一、逐项回复

### P0-1：LocalAgreement 基线没有计算 WER/CER —— **接受，已修复**

**修改**：
- `experiments/scripts/run_exp_baseline_la.py` 直接复用 `run_exp_quality.py` 的 `normalize_text` / `wer` / `cer` / `zh_to_word_seq`，与实验三完全同一归一化口径（不是重写，是 import）；
- 每个 LA 样本以 `sample.text` 为 reference、`transcribed_text` 为 hypothesis 计算 WER/CER（中文按 `zh_to_word_seq` 逐字口径算 WER，与实验三一致），写入：逐样本 JSON、`la_summary_*.csv`、`la_statistics_*.csv`（含 group/dataset/overall 三种 scope）；
- 空转写单独标记 `error="asr_no_text"`，不参与统计（统计聚合只取 `error` 为空的记录）。

**验证**：
- 本机冒烟（tiny + Qwen2.5-0.5B，crosswoz_10040_turn1）：JSON 含 `wer=0.1136, cer=0.1333`，CSV 含对应列，统计 CSV 含 `wer_mean/cer_mean`；
- 交叉核对：用 `run_exp_quality.py` 的函数对同一样本独立重算，wer/cer 与脚本输出逐位一致。

### P0-2：`final_segment_commit_time` 的实际含义与计划定义不一致 —— **接受，已修复**

按评审推荐方案（双时间点、命名消歧）实现：

- `final_speech_segment_commit_time`：最后一个**含语音**段（VAD 闭段；无尾静音时为含语音的 flush 段）入 `audio_segment_queue` 的时刻。**论文端点等待口径**：
  `endpoint_detection_wait = final_speech_segment_commit_time − speech_end_time`；
- `final_is_final_segment_enqueue_time`：flush 产生的 `is_final=True` 段入队时刻（审计用，`final_enqueue_wait = final_is_final_segment_enqueue_time − speech_end_time`）；
- 旧字段 `final_segment_commit_time` 已删除（E5 尚未正式运行，无历史数据兼容问题）；
- `GPU_EXPERIMENT_HANDOFF.md` DEV-1/E5 与 `EXPERIMENT_DESIGN.md` §6.5 已同步为新口径。

**验证**：`--append-silence-ms 2000` 冒烟：两字段均非零；`audio_end_time − speech_end_time = 2.018 s ≈ 2 s`；`endpoint_detection_wait = 0.044 s`，`final_enqueue_wait = 2.080 s`，两个 wait 可分别计算、语义清晰。

### P0-3：LA 空尾片段可能导致 LLM 增量缓存失败 —— **部分接受（异常路径澄清 + 数据质量部分已修复）**

**技术澄清（不接受的部分）**：评审所述"`tail == ""` 且有增量文本时 `_add_stream_prompt()` 抛出'要添加的文本为空'异常"的路径**实际不可达**。`StreamLLMInference.cache_prompt()` 在非首次且 `is_end=True` 时执行 `prompt += self.generation_prompt`，送入 `_add_stream_prompt` 的必然是非空的 generation prompt（`<|im_start|>assistant\n`），不会触发空文本异常。原 `run_exp_latency.py` 的 `text_queue.put(("", True))` 走同一路径，全部历史实验均未触发。已在 LA 脚本对应位置加注释说明该安全性。

**接受的部分**：评审指出的"全程无文本会静默生成无意义结果"属实。修复：LA 结果中无有效提交文本时标记 `error="asr_no_text"`（与 `run_exp_quality.py` 既有约定一致），统计聚合自动排除，不再静默产生看似正常的质量/延迟数字。

**关于验收标准中 3 种测试的说明**：case 1（有增量、tail 空）经上述分析为安全路径，冒烟未出现异常；case 2（无增量、tail 非空）为首轮 `_init_kv_cache` 路径，安全；case 3（全程无文本）已由 `asr_no_text` 覆盖。三种情况均不存在空文本进入 `_add_stream_prompt` 的路径。

### P0-4：TTS 测量脚本会静默使用截断的 response_preview —— **接受，已修复**

- 默认行为：E4 结果缺少有效 `full_response` 时直接 `SystemExit` 并报错 `"请用 --save-full-response 重跑 E4，或显式加 --allow-preview-fallback"`；
- 新增 `--allow-preview-fallback`（默认关闭）：仅对**键缺失**（旧版 E4 未开启 `--save-full-response`）的记录开放，且 CSV 新增 `text_source` 列标记 `preview`，不与 `full_response` 混淆；
- **键存在但为空**的记录判定为无效（E4 运行异常），即使允许 fallback 也不采用——对应评审验收用例 3；
- 输出 CSV 旁写 `*.runinfo.md` 记录是否允许 fallback 及全部请求配置。

**验证**（4 个回归用例全过）：
1. 仅 preview（键缺失）→ 默认非零退出 ✓；2. 含 `full_response` → 正常抽取且 `text_source=full_response` ✓；3. `full_response=""` + 允许 fallback → 判无效、未 fallback ✓；4. 键缺失 + 允许 fallback → 采用 preview 且标记 ✓。

### P1-1：Table IV 重算缺少三模式完整性校验 —— **接受，已修复**

`recompute_stats.py` 的 `table4()` 在统计前建立 `sample_id -> mode -> result` 索引并强制校验：每样本恰好 baseline/streaming_asr_only/full_streaming 三种模式各一条、全部无 error、`audio_duration` 三模式一致。任一违反打印违规样本详情并 `SystemExit(1)`。

**验证**：现有 exp2 JSON 通过（498 样本 × 3 模式）；负例测试（删除一条 full_streaming 记录）非零退出并指明 `crosswoz_11613_turn2` 缺 full_streaming ✓。输出中每组三模式 n 完全一致（108/150/240）。

### P1-2：Table III 的 paired filtering 改变了 baseline 样本口径，需要显式记录 —— **接受，已修复**

- 新增 `experiments/results/revision/r1_stats/table3_filter_manifest.json`：排除样本 ID、排除原因（runtime error / hang outlier 及触发值）、每组 原始 n / 排除 n / 最终 n；
- 口径说明（"为保证模式间成对比较，重算时排除对应模式不完整的样本，baseline 的样本数和均值可能较旧表轻微变化"）写入 `EXPERIMENT_DESIGN.md` §6.1，论文/回信将按此表述，不会声称所有旧均值未变（实际变化：Extra Long baseline 6753.43 → 6745.57 ms，即排除 crosswoz_7310_turn10 所致）。

### P1-3：LocalAgreement 前缀修正时可能发生状态失配 —— **接受，已修复**

- `LocalAgreementStreamer` 新增 `divergence_events`：当 `len(agreed) < n_committed` 时记录事件（时间、已提交数、公共前缀长、双方词序列头部），并 `logger.warning`；
- 恢复策略（显式选择，理由附后）：**不回退 `n_committed`**——已提交文本 append-only 不可撤销，回退会造成重复提交；分歧通常是暂态的，后续轮次公共前缀可自然重新延伸越过 `n_committed` 恢复提交；`flush()` 保证流末剩余假设照常提交，不会永久卡死；
- `divergence_count` 写入 LA 结果 JSON 与 `la_summary_*.csv`，作为论文归因讨论的观测数据。

**验证**（mock 测试，模拟评审给的类似序列）：`hello world` → `hello world again` → `hello wurst again` → `hello wurst again more`。结果：第 3 轮记录 1 次分歧事件（agreed=1 < committed=2）；第 4 轮公共前缀重延伸恢复提交（`again`）；flush 提交 `more`；无死锁、无重复提交、无静默丢失 ✓。

### P1-4：实验方法改变但没有同步更新 EXPERIMENT_DESIGN.md —— **接受，已补**

`experiments/EXPERIMENT_DESIGN.md` 新增"六、CISR 审稿修订实验"章节，覆盖评审点名的全部方法：R1 分位数与重复测量（含过滤规则、Table V pooled 历史口径）、R2 真实语音及增强变体、R3 LA-2 模型/解码/提交规则、R4 append-only 观测、R6 三个时间字段定义与 TTFA 预算组成。该章声明为方法学定义的唯一权威来源，与 PLAN/HANDOFF 交叉引用。

### P1-5：TTS 运行配置硬编码 —— **接受，已修复**

- 支持环境变量：`TTS_URL`、`TTS_SPK_ID`、`TTS_SPEED`、`TTS_SAMPLE_RATE`、`TTS_BITS_PER_SAMPLE`；
- CLI 参数 `--url/--spk-id/--speed` 可覆盖 env；默认值保持论文数据生成口径（晓伊 / 0.8 / 22050Hz 16bit mono）；
- 输出 CSV 旁写 `*.runinfo.md`：完整命令行、服务地址、spk_id、speed、PCM 解释、输入来源、fallback 开关、条数。

---

## 二、评审 §2"已确认没有问题"事项的跟进

- **§2.1 Table V**：维持原数字，`recompute_stats.py` 的 `pooled` 行可精确复核（1327.48/1224.96/1086.16）。修改稿若引用 streaming-only 明细会显式说明口径差异。
- **§2.2 Table IV**：按"可复现规则重算、不声称复原旧手工排除"的表述执行。新增逐样本数据源落盘（回应"旧排除信息未留下"的历史问题）：
  - `experiments/results/exp2_ablation/exp2_gains_clean.csv`：498 条保留样本三模式数据，列布局与原 gains CSV 一致；
  - `exp2_gains_exclusions.csv`：7 条排除样本的原因与触发值；
  - 生成脚本 `make_exp2_clean_source.py` 三重验证：样本集合重合（原 502 = 保留 498 + 挂起 4）、逐样本数值与原 CSV 2dp 完全一致、分组均值与重算口径一致。

## 三、回归测试汇总（全部通过）

| 测试 | 结果 |
|---|---|
| 7 个改动文件 `py_compile` | ✓ |
| `recompute_stats.py` 全量重跑（含 P1-1 校验、P1-2 manifest） | ✓ |
| P1-1 负例（删一条 full_streaming） | ✓ 非零退出并指明样本 |
| P0-4 四用例（preview-only / full / empty-full / 键缺失+fallback） | ✓ |
| P1-3 mock 分歧恢复序列 | ✓ |
| LA 冒烟（含 WER/CER、交叉核对 run_exp_quality 口径） | ✓ |
| endpoint 冒烟（双时间字段非零、audio_end−speech_end≈2s） | ✓ |

## 四、请复审时重点关注

1. P0-3 的技术澄清是否成立（`cache_prompt` 的 `is_end=True` 分支，`stream_llm_inference.py:188-190`）；
2. P0-2 双字段命名与论文采用口径（`EXPERIMENT_DESIGN.md` §6.5）；
3. `exp2_gains_clean.csv` 三重验证是否满足"后续复查与重新统计"的要求；
4. 评审状态表（原文 §7）各项"已修复"描述与实际代码是否一致。
